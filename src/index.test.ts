// Uncomment the code below and write your tests
import {str} from '.';

describe('Scenario 1', () => {
  test('test 1', async () => {
    expect(str).toBe("Hello");
  });

  test('test 2', () => {
    expect(str).toBe("Hello");
  });
});
